spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

  datasource:
      username:
      password:
      url:
      driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

es-info:
  host: ec2-3-39-36-0.ap-northeast-2.compute.amazonaws.com
  port: 9200
